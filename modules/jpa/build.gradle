plugins {
    id 'java-library'
}

description = 'jpa-aggregator-module'

dependencies {
    api project(':modules:jpa-auditing')
    api project(':modules:jpa-querydsl')

    testRuntimeOnly 'com.h2database:h2'
}
